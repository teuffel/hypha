{
  "name": "hypha-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thin MCP server bridging opencode to a Hypha graph via the logseq CLI (RTC-synced writes)",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "zod": "^3.25.0"
  }
}
